import { AppBar, Toolbar } from "@mui/material";

const PrimaryAppBar = () => {
  return (
    <AppBar>
      <Toolbar variant="dense">This is the app bar</Toolbar>
    </AppBar>
  );
};

export default PrimaryAppBar;
